<?xml version="1.0" encoding="utf-8"?>
<!--
  Launcher icon.

  An adaptive icon is used unconditionally: minSdk is 28, so every device this
  application runs on supports one, and no -v26 qualified fallback is needed.
-->
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <background android:drawable="@color/omni_background" />
    <foreground android:drawable="@drawable/omni_mark" />
</adaptive-icon>
